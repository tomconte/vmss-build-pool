name: build-image

on:
  push:
    paths: 
      - 'agent-image.json'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2

    - name: Check JSON syntax
      run: cat agent-image.json | jq || exit 1
